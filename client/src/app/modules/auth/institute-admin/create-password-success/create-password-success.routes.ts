import { Routes } from '@angular/router';
import { CreatePasswordSuccessComponent } from './create-password-success.component';

export default [
    {
        path: '',
        component: CreatePasswordSuccessComponent,
    },
] as Routes;
